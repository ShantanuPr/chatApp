{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "chrome",
        "request": "launch",
        "name": "Launch Chrome against localhost",
        "url": "http://localhost:3000", // Ensure this matches your React app's URL
        "webRoot": "${workspaceFolder}/src",
        "sourceMaps": true // Ensure source maps are enabled
      }
    ] 
  }
  